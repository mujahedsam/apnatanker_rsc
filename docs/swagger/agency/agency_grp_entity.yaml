/agencygrp/{id}:
  get:
    tags:
      - AgencyGRP
    summary: "Finds Agency group by id"
    description:
      Agency Group Entity.
    parameters:
      - name: id
        in: path
        required: true
        type: string
    responses:
        200:
            description: "status:1 is success; status: 0 is failure;"
        400:
            description: "Invalid status value"
        500:
            description: "Internal server error"
